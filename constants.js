const IP = 'localhost';
const PORT = 50541;

const moveUp = 'w';
const moveDown = 's';
const moveRight = 'd';
const moveLeft = 'a';

module.exports = {
  IP,
  PORT,
  moveUp,
  moveDown,
  moveRight,
  moveLeft
};